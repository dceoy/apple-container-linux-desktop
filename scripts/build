#!/usr/bin/env sh
set -eu

if [ -f .env ]; then
  set -a
  # shellcheck source=/dev/null
  . ./.env
  set +a
fi

IMAGE="${IMAGE:-linux-desktop:latest}"

container build \
  --platform linux/arm64 \
  --tag "${IMAGE}" \
  .
