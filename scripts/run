#!/usr/bin/env sh
# Compatibility helper for the historical scripts/run entrypoint.
# Prefer `make up` for new usage.
set -eu

SCRIPT_DIR=$(CDPATH='' cd -- "$(dirname -- "$0")/.." && pwd)

exec make -C "${SCRIPT_DIR}" up "$@"
